ProgB:

DECLARATIONS

a, b, c OF TYPE INTEGER;
d, e OF TYPE REAL;
f OF TYPE CHARACTER;

CODE

  READ(a);
  READ( b );
  IF a > b THEN
    WRITE ('A')
  ELSE
    WRITE ( 'B')
  ENDIF;
  NEWLINE;
  READ (d) ;
  d * 2.3 -> e;
  WRITE(e);
  NEWLINE;
  READ(f);
  WRITE(f);
  NEWLINE

ENDP ProgB.
